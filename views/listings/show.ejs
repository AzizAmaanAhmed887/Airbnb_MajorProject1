<% layout('layouts/boilerplate.ejs') %>

<div class="row mt-3">
  <div class="col-8 offset-3">
    <h3><b><%= listing.title %></b></h3>
  </div>
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const deleteBtn = document.getElementById('delete-listing-btn');
      if (deleteBtn) {
        deleteBtn.addEventListener('click', function (e) {
          if (!confirm('Are you sure you want to delete this listing?')) {
            e.preventDefault();
          }
        });
      }
    });
  </script>
  <div class="card col-6 offset-3 show-card listing-card">
    <img
      src="<%= listing.image.url %>"
      class="card-img-top show-image"
      alt="listing_image"
    />
    <div class="card-body">
      <p class="card-text">Owner: <b><%= listing.owner.username %></b></p>
        <p class="card-text">description: <%= listing.description %> </p>
        <p class="card-text">price: &#8377; <%= listing.price %> per night </p>
        <p class="card-text">Location: <%= listing.location %> </p>
        <p class="card-text">Country: <%= listing.country %> </p>
    </div>
  </div>

  <br />
  <% if(currUser && currUser._id.equals(listing.owner._id)) { %>
    <div class="btns">
      <a
      href="/listings/<%= listing._id %>/edit"
      class="btn btn-dark col-1 offset-3 edit-btn"
      >Edit</a
    >
    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE" class="d-inline">
      <input type="hidden" name="_method" value="DELETE" />
      <button
      class="btn btn-dark offset-5"
      type="submit"
      id="delete-listing-btn"
      >
        Delete
        </button>
        </form>
        </div>
   <% } %> 

  <div class="col-8 offset-3 mb-3">
    <!-- Taking review(rating + comment) from user -->
    <hr />
    <h4>Leave a Review</h4>
    <form action="/listings/<%= listing._id %>/reviews" method="POST" novalidate class="needs-validation">
      <div class="mb-3 mt-3">
        <label for="rating" class="form-label">Rating</label>
        <input
          class="form-range"
          type="range"
          name="review[rating]"
          id="rating"
          min="1"
          max="5"
        />
      </div>
      <div class="mb-3 mt-3">
        <label for="comment" class="form-label">Comment</label>
        <textarea
          name="review[comment]"
          id="comment"
          rows="3"
          class="form-control"
          required
        ></textarea>
        <div class="invalid-feedback">
          Please provide a comment for your review.
      </div>
      <button type="submit" class="btn btn-outline-dark">Submit</button>
    </form>

    <!-- Displaying all reviews -->
    <hr>
    <!-- styling reviews -->
    <p><b>All reviews:</b></p>
    <div class="row">
      <% if (listing.reviews.length === 0) { %>
      <p>No reviews yet. Be the first to review!</p>
      <% } else { %>
      <% for (let review of listing.reviews) { %>
        <div class="card col-5 ms-3 mb-3">
          <div class="card-body">
            <h5 class="card-title"> <b><%= review.author || review.username || 'Anonymous' %></b> </h5>
            <p class="card-text"><%= review.comment %></p>
            <p class="card-text">Rating: <%= review.rating %> ⭐</p>
            <!-- Now writing logic to delete a review -->
            <form method="POST" action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" class="ms-3">
            <!-- Remove this line: <input type="hidden" name="_method" value="DELETE" /> -->
            <button class="btn btn-sm btn-dark">Delete</button>
            </form>
          </div>
        </div>
        <% } %>
        <% } %>
      </div>
    </div>
</div>
